name: data
description: Shared data layer implementations and models for GRRADO
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  json_annotation: ^4.8.0
  dio: ^5.0.0
  core:
    path: ../core
  domain:
    path: ../domain

dev_dependencies:
  flutter_test:
    sdk: flutter
  json_serializable: ^6.6.0
  build_runner: ^2.3.0
